# Google Cloud Build - CI/CD Configuration
# Triggered automatically on git push to main branch
# Based on: https://cloud.google.com/build/docs/deploying-builds/deploy-cloud-run

steps:
  # Build Enrichment Service
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-enrichment'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:$SHORT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:latest'
      - './services/enrichment'
  
  # Push Enrichment Service to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-enrichment'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:$SHORT_SHA'
  
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:latest'
  
  # Deploy Enrichment Service to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-enrichment'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'enrichment-service'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:latest'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--set-secrets=GEMINI_API_KEY=gemini-api-key:latest,PERPLEXITY_API_KEY=perplexity-api-key:latest'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--timeout=300'
      - '--max-instances=10'

# Store images in Artifact Registry
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:$SHORT_SHA'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/muir-services/enrichment-service:latest'

# Build timeout
timeout: 1200s

# Substitutions
substitutions:
  _SERVICE_NAME: enrichment-service
  _REGION: us-central1
